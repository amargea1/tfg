{% extends 'base.html.twig' %}

{% block head %}
    <link rel="stylesheet" href="{{ asset('css/stylesForm.css') }}">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
{% endblock %}

{% block body %}
    <div class="dashboard-container">
        <aside class="sidebar">
            <img src="{{ asset('image/logo.png') }}" alt="Logo" class="logo">
            <ul class="menu">
                <li>
                    <a href="{{ path('admin_panel') }}">
                        <i class="fa-solid fa-gauge"></i> Panel de control
                    </a>
                </li>

                <li>
                    <a href="{{ path('socio_ver') }}">
                        <i class="fa-solid fa-users"></i> Socios
                    </a>
                </li>
                <li>
                    <a href="{{ path('cuota_ver') }}">
                        <i class="fa-solid fa-coins"></i> Cuotas
                    </a>
                </li>
                <li>
                    <a href="{{ path('consultas_ver') }}">
                        <i class="fa-solid fa-comments"></i> Consultas
                    </a>
                </li>
                <li>
                    <a href="{{ path('reclamacion_ver') }}">
                        <i class="fa-solid fa-file-lines"></i> Reclamaciones
                    </a>
                </li>
                <li>
                    <a href="{{ path('admin_panel') }}">
                        <i class="fa-solid fa-gear"></i> Configuración
                    </a>
                </li>

            </ul>
        </aside>

        <main class="rec-panel">
            <div class="top-bar text-end">
                <a href="{{ path('admin_panel') }}" class="btn btn-outline-info me-3">Volver</a>
                <a href="{{ path('app_logout') }}" class="btn btn-outline-danger">Cerrar sesión</a>
            </div>

            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-{{ label }} alert-dismissible fade show" role="alert">
                        {{ message }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Cerrar"></button>
                    </div>
                {% endfor %}
            {% endfor %}

            <div class="container my-5">
                <div class="card shadow p-4">
                    <h2 class="text-center mb-4">Abrir reclamación</h2>

                    {% for mensaje in app.flashes('error') %}
                        <div class="alert alert-danger">{{ mensaje }}</div>
                    {% endfor %}

                    {{ form_start(form) }}

                    <div class="mb-3">
                        {{ form_label(form.fechaApertura) }}
                        {{ form_widget(form.fechaApertura, {'attr': {'class': 'form-control'}}) }}
                        {% if form.fechaApertura.vars.errors|length > 0 %}
                            <div class="invalid-feedback d-block">
                                {{ form_errors(form.fechaApertura) }}
                            </div>
                        {% endif %}
                    </div>

                    <div class="mb-3">
                        {{ form_label(form.atencion) }}
                        {{ form_widget(form.atencion, {'attr': {'class': 'form-control'}}) }}
                        {% if form.atencion.vars.errors|length > 0 %}
                            <div class="invalid-feedback d-block">
                                {{ form_errors(form.atencion) }}
                            </div>
                        {% endif %}
                    </div>

                    <div class="mb-3">
                        {{ form_label(form.numeroSocio) }}
                        {{ form_widget(form.numeroSocio, {'attr': {'class': 'form-control'}}) }}
                        {% if form.numeroSocio.vars.errors|length > 0 %}
                            <div class="invalid-feedback d-block">
                                {{ form_errors(form.numeroSocio) }}
                            </div>
                        {% endif %}
                    </div>

                    <div class="mb-3">
                        {{ form_label(form.esFamiliar) }}
                        {{ form_widget(form.esFamiliar, {'attr': {'class': 'form-check-input ms-2'}}) }}
                        {% if form.esFamiliar.vars.errors|length > 0 %}
                            <div class="invalid-feedback d-block">
                                {{ form_errors(form.esFamiliar) }}
                            </div>
                        {% endif %}
                    </div>

                    <div class="mb-3">
                        {{ form_label(form.familiar) }}
                        {{ form_widget(form.familiar, {'attr': {'class': 'form-control'}}) }}
                        {% if form.familiar.vars.errors|length > 0 %}
                            <div class="invalid-feedback d-block">
                                {{ form_errors(form.familiar) }}
                            </div>
                        {% endif %}
                    </div>

                    <div class="mb-3">
                        {{ form_label(form.sector) }}
                        {{ form_widget(form.sector, {'attr': {'class': 'form-control'}}) }}
                        {% if form.sector.vars.errors|length > 0 %}
                            <div class="invalid-feedback d-block">
                                {{ form_errors(form.sector) }}
                            </div>
                        {% endif %}
                    </div>

                    <div class="mb-3">
                        {{ form_label(form.prioridad) }}
                        {{ form_widget(form.prioridad, {'attr': {'class': 'form-control'}}) }}
                        {% if form.prioridad.vars.errors|length > 0 %}
                            <div class="invalid-feedback d-block">
                                {{ form_errors(form.prioridad) }}
                            </div>
                        {% endif %}
                    </div>

                    <div class="mb-3">
                        {{ form_label(form.asunto) }}
                        {{ form_widget(form.asunto, {'attr': {'class': 'form-control'}}) }}
                        {% if form.asunto.vars.errors|length > 0 %}
                            <div class="invalid-feedback d-block">
                                {{ form_errors(form.asunto) }}
                            </div>
                        {% endif %}
                    </div>

                    <div class="mb-3">
                        {{ form_label(form.reclamacion) }}
                        {{ form_widget(form.reclamacion, {'attr': {'class': 'form-control', 'rows': '12'}}) }}
                        {% if form.reclamacion.vars.errors|length > 0 %}
                            <div class="invalid-feedback d-block">
                                {{ form_errors(form.reclamacion) }}
                            </div>
                        {% endif %}
                    </div>

                    <div class="d-flex justify-content-between mt-4">
                        <button type="submit" class="btn-orange">
                            Guardar
                        </button>


                    </div>

                    {{ form_end(form) }}
                </div>
            </div>
        </main>
    </div>

{#    <script>#}
{#        document.addEventListener('DOMContentLoaded', function () {#}
{#            const checkbox = document.querySelector('#{{ form.esFamiliar.vars.id }}');#}
{#            const familiarFields = document.querySelector('#familiar_fields');#}

{#            if (checkbox.checked) {#}
{#                familiarFields.style.display = 'block';#}
{#            }#}

{#            checkbox.addEventListener('change', function () {#}
{#                familiarFields.style.display = checkbox.checked ? 'block' : 'none';#}
{#            });#}
{#        });#}
{#    </script>#}
{% endblock %}
