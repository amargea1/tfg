{% extends 'base.html.twig' %}

{% block head %}
    <link rel="stylesheet" href="{{ asset('css/stylesForm.css') }}">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
          crossorigin="anonymous" referrerpolicy="no-referrer"/>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"/>
{% endblock %}

{% block hb %}
    <div class="d-flex ms-auto">
        <a href="{{ path('admin_panel') }}" class="btn btn-outline-info me-3">Volver</a>
        <a href="{{ path('app_logout') }}" class="btn btn-outline-danger">Cerrar sesión</a>
    </div>
{% endblock %}

{% block main %}
    <main class="main-panel">
        <div class="container my-5">
            <div class="card shadow p-4">
                <h2 class="text-center mb-4">Registrar admin</h2>
                {{ form_start(form) }}

                <div class="mb-3">
                    {{ form_label(form.rol) }}
                    {{ form_widget(form.rol, {'attr': {'class': 'form-control'}}) }}
                    {% if form.rol.vars.errors|length > 0 %}
                        <div class="invalid-feedback d-block">
                            {{ form_errors(form.rol) }}
                        </div>
                    {% endif %}
                </div>

                <div class="mb-3">
                    {{ form_label(form.especialidad) }}
                    {{ form_widget(form.especialidad, {'attr': {'class': 'form-control'}}) }}
                    {% if form.especialidad.vars.errors|length > 0 %}
                        <div class="invalid-feedback d-block">
                            {{ form_errors(form.especialidad) }}
                        </div>
                    {% endif %}
                </div>

                <div class="mb-3">
                    {{ form_label(form.fechaCreacion) }}
                    {{ form_widget(form.fechaCreacion, {'attr': {'class': 'form-control'}}) }}
                    {% if form.fechaCreacion.vars.errors|length > 0 %}
                        <div class="invalid-feedback d-block">
                            {{ form_errors(form.fechaCreacion) }}
                        </div>
                    {% endif %}
                </div>

                <div class="mb-3">
                    {{ form_label(form.nombre) }}
                    {{ form_widget(form.nombre, {'attr': {'class': 'form-control'}}) }}
                    {% if form.nombre.vars.errors|length > 0 %}
                        <div class="invalid-feedback d-block">
                            {{ form_errors(form.nombre) }}
                        </div>
                    {% endif %}
                </div>

                <div class="mb-3">
                    {{ form_label(form.apellidos) }}
                    {{ form_widget(form.apellidos, {'attr': {'class': 'form-control'}}) }}
                    {% if form.apellidos.vars.errors|length > 0 %}
                        <div class="invalid-feedback d-block">
                            {{ form_errors(form.apellidos) }}
                        </div>
                    {% endif %}
                </div>

                <div class="mb-3">
                    {{ form_label(form.dni) }}
                    {{ form_widget(form.dni, {'attr': {'class': 'form-control'}}) }}
                    {% if form.dni.vars.errors|length > 0 %}
                        <div class="invalid-feedback d-block">
                            {{ form_errors(form.dni) }}
                        </div>
                    {% endif %}
                </div>

                <div class="mb-3">
                    {{ form_label(form.fechaNacimiento) }}
                    {{ form_widget(form.fechaNacimiento, {'attr': {'class': 'form-control'}}) }}
                    {% if form.fechaNacimiento.vars.errors|length > 0 %}
                        <div class="invalid-feedback d-block">
                            {{ form_errors(form.fechaNacimiento) }}
                        </div>
                    {% endif %}
                </div>

                <div class="mb-3">
                    {{ form_label(form.sexo) }}
                    {{ form_widget(form.sexo, {'attr': {'class': 'form-control'}}) }}
                    {% if form.sexo.vars.errors|length > 0 %}
                        <div class="invalid-feedback d-block">
                            {{ form_errors(form.sexo) }}
                        </div>
                    {% endif %}
                </div>

                <div class="mb-3">
                    {{ form_label(form.direccion) }}
                    {{ form_widget(form.direccion, {'attr': {'class': 'form-control'}}) }}
                    {% if form.direccion.vars.errors|length > 0 %}
                        <div class="invalid-feedback d-block">
                            {{ form_errors(form.direccion) }}
                        </div>
                    {% endif %}
                </div>

                <div class="mb-3">
                    {{ form_label(form.localidad) }}
                    {{ form_widget(form.localidad, {'attr': {'class': 'form-control'}}) }}
                    {% if form.localidad.vars.errors|length > 0 %}
                        <div class="invalid-feedback d-block">
                            {{ form_errors(form.localidad) }}
                        </div>
                    {% endif %}
                </div>

                <div class="mb-3">
                    {{ form_label(form.provincia) }}
                    {{ form_widget(form.provincia, {'attr': {'class': 'form-control'}}) }}
                    {% if form.provincia.vars.errors|length > 0 %}
                        <div class="invalid-feedback d-block">
                            {{ form_errors(form.provincia) }}
                        </div>
                    {% endif %}
                </div>

                <div class="mb-3">
                    {{ form_label(form.codigoPostal) }}
                    {{ form_widget(form.codigoPostal, {'attr': {'class': 'form-control'}}) }}
                    {% if form.codigoPostal.vars.errors|length > 0 %}
                        <div class="invalid-feedback d-block">
                            {{ form_errors(form.codigoPostal) }}
                        </div>
                    {% endif %}
                </div>

                <div class="mb-3">
                    {{ form_label(form.telefono) }}
                    {{ form_widget(form.telefono, {'attr': {'class': 'form-control'}}) }}
                    {% if form.telefono.vars.errors|length > 0 %}
                        <div class="invalid-feedback d-block">
                            {{ form_errors(form.telefono) }}
                        </div>
                    {% endif %}
                </div>

                <div class="mb-3">
                    {{ form_label(form.email) }}
                    {{ form_widget(form.email, {'attr': {'class': 'form-control'}}) }}
                    {% if form.email.vars.errors|length > 0 %}
                        <div class="invalid-feedback d-block">
                            {{ form_errors(form.email) }}
                        </div>
                    {% endif %}
                </div>

                <div class="mb-3">
                    {{ form_label(form.username) }}
                    {{ form_widget(form.username, {'attr': {'class': 'form-control'}}) }}
                    {% if form.username.vars.errors|length > 0 %}
                        <div class="invalid-feedback d-block">
                            {{ form_errors(form.username) }}
                        </div>
                    {% endif %}
                </div>

                <div class="mb-3">
                    {{ form_label(form.password) }}
                    {{ form_widget(form.password, {'attr': {'class': 'form-control'}}) }}
                    {% if form.password.vars.errors|length > 0 %}
                        <div class="invalid-feedback d-block">
                            {{ form_errors(form.password) }}
                        </div>
                    {% endif %}
                </div>
                <div class="d-flex justify-content-between mt-4">
                    <button type="submit" class="btn-orange">
                        Guardar
                    </button>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </main>
{% endblock %}

